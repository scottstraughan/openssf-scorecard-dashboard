import { ScorecardCheck } from './scorecard-check.model';

/**
 * Model that represents a scorecard's check details.
 */
export interface ScorecardCheckDetails {
  details: string
  check: ScorecardCheck
}
